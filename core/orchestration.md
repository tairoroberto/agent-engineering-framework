# Orchestration

Progressive load: project rules -> manifest -> relevant framework capability -> targeted memory -> relevant source.

Orchestrator decides single-agent or delegated execution. Dispatch includes goal, bounded ownership, dependencies, expected evidence, verification gate, write/commit authority. Parallel work needs independent dependencies and disjoint write boundaries; shared state/configuration stays sequential by default.
