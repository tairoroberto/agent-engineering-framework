# Claude Code adapter

Adapter scope only: discover project `AGENTS.md`, `CLAUDE.md`, `.agent-framework.toml`, managed workflow assets, `.claude/agents/`, `.claude/commands/`, and the project-local model catalog. Claude model, effort, turn-limit, and permission syntax remain execution details. Pass only the compact role capsule; never reconstruct work from a transcript.
