# Delegation

Use one role per bounded responsibility. Explorer is read-first. Implementer changes only assigned paths. Reviewer is fresh and read-only. Verifier runs applicable project/profile commands and acceptance checks.

Dispatch minimum context: goal, rules/paths, task boundary, dependencies, outcome, gate, authority. Return compact evidence, not reasoning or transcripts.
