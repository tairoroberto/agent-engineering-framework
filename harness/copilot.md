# GitHub Copilot adapter

Adapter scope only: discover project `AGENTS.md`, `.agent-framework.toml`, managed workflow assets, `.github/agents/`, `.github/instructions/`, `.github/prompts/`, and the project-local model catalog. Copilot model, effort, tool, and fallback syntax remain execution details. A named provider requires an effective-model receipt matching the approved dispatch.
